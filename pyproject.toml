[project]
name = "taskflow-daily"
version = "0.1.0"
description = "Daily-built Python task management utilities."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
